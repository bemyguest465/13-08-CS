#include<stdio.h>
void main() {
    int l, h;
    printf("Enter the length: ");
    scanf("%d", &l);
    printf("Enter the height: ");
    scanf("%d", &h);
    printf("The area is %d", .5*l*h);
    printf("The perimeter is %d", 3*l);
}